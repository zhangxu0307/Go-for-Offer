#include <iostream>
#include <stack>
using namespace std;

bool IsPopOrder(vector<int> pushV,vector<int> popV)
{
    stack<int> s;
    int index = 0;
    while(popV[index] == s.top())
    {

    }
}

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}